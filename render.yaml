services:
  - type: web
    name: coding-interview-platform
    env: docker
    dockerfilePath: ./Dockerfile
    region: singapore
    plan: free
    autoDeploy: true
    envVars:
      - key: PORT
        value: 3001
